# from sktime.performance_metrics.forecasting import MeanAbsoluteScaledError


def mase(predicted, target, train) -> float:
    #_mase = MeanAbsoluteScaledError()
    # y_true = target
    # y_pred = predicted
    # return _mase(y_true, y_pred, y_train=train)
    raise ValueError("Not implemented!")
